{
  "name": "mars-proxy",
  "version": "1.0.0",
  "description": "Anthropic API proxy for Mars Maps AI class",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "logs":   "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
